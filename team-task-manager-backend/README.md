# Team Task Manager Backend

Express API for the team task manager application.

## Scripts

```bash
npm run dev
npm run build
npm start
```

The development server runs `src/index.ts`. By default, the API listens on port `5000`.
